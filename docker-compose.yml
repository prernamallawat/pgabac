services:
  postgres:
    build: .
    container_name: pg-abac
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: university_abac
    ports:
      - "5434:5432"
    volumes:
      - pgabac_data:/var/lib/postgresql/data

volumes:
  pgabac_data:
